# Intentionally empty — Cloudflare Pages serves index.html for all
# unmatched routes by default (SPA mode). No explicit redirects needed.
# The /* /index.html 200 pattern triggers Cloudflare's infinite loop
# detection and gets silently ignored, so we don't use it.
